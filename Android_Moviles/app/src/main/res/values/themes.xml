<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal -->
    <style name="Theme.JaniSPAKotlinAPP" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Colores principales -->
        <item name="colorPrimary">@color/colorAccent</item> <!-- Azul petróleo para la barra superior -->
        <item name="colorPrimaryDark">@color/colorAccent</item> <!-- Azul petróleo para status bar -->
        <item name="colorAccent">@color/colorAccent</item> <!-- Azul petróleo para botones, etc. -->

        <!-- Colores del texto -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>

        <!-- Fondo general -->
        <item name="android:windowBackground">@color/colorBackground</item>

        <!-- Barra de estado -->
        <item name="android:statusBarColor">@color/colorAccent</item>

        <!-- Barra inferior (navegación virtual) -->
        <item name="android:navigationBarColor">@color/colorAccent</item>
    </style>

</resources>
